server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: all /provnote/* routes fall back to index.html
    location /provnote/ {
        try_files $uri $uri/ /provnote/index.html;
    }

    # Redirect root to /provnote/
    location = / {
        return 301 /provnote/;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
