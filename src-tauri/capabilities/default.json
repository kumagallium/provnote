{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Graphium デスクトップアプリのデフォルト権限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/graphium-server",
          "sidecar": true
        }
      ]
    },
    "shell:allow-kill"
  ]
}
